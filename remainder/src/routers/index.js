const productRouter = require('./products');
const shopRouter = require('./shops');
const remainderRouter = require('./remainders');

module.exports = {
  productRouter,
  shopRouter,
  remainderRouter,
};
