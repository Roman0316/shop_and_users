const productController = require('./product');
const shopController = require('./shop');
const remainderController = require('./remainder');

module.exports = {
  productController,
  shopController,
  remainderController,
};
